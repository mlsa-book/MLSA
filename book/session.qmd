# Session Info {#sec-app-session}

If you would like to reproduce the results in this book please note the seeds used and the `sessionInfo` at the time of publication:

```{r, echo = FALSE}
sessionInfo()
```